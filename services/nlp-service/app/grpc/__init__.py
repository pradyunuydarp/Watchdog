"""gRPC preparation package for the NLP service.

The package contains proto-shaped dataclasses and a transport adapter layer so
the service can adopt generated protobuf classes later without changing the
domain logic.
"""

